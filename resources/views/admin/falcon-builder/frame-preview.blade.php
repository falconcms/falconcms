{{-- Read-only header/footer preview rendered through the real theme layout (so it inherits
     all theme CSS) and embedded as an iframe inside the page/post/CPT/product builder.
     $builderFramePart ('header'|'footer') makes the theme layout output only that part. --}}
@extends(falcon_theme_view('layouts.app'))

@section('content')@endsection
